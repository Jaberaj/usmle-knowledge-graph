# Contributing

Medical-content PRs identify changed tables and IDs, reference and medical review, treatment/emergency/algorithm changes, contract impact, migration need, and validation commands. Run `make all`; never manually edit generated directories.
